package instrumenter

func switchStmt(expr int, cond bool, s string) {

	switch {

	case gobcoCover(0, expr == 5):

	case cond:

	}

	switch gobco0 := s; {
	case gobcoCover(1, gobco0 == "one"),
		gobcoCover(2, gobco0 == "two"),
		gobcoCover(3, gobco0 == "three"):
	}

	switch gobco1 := s + "suffix"; {
	case gobcoCover(4, gobco1 == "one"),
		gobcoCover(5, gobco1 == "two"),
		gobcoCover(6, gobco1 == ""+s):
	}
	switch {
	case true:

		s = "prefix" + s
		gobco2 := s + "suffix"
		switch {
		case gobcoCover(7, gobco2 == "prefix.a.suffix"):
		}
	}
	switch {
	case true:

		s := "prefix" + s
		gobco3 := s + "suffix"
		switch {
		case gobcoCover(8, gobco3 == "prefix.a.suffix"):
		}
	}

	switch s := "prefix" + s; {
	case gobcoCover(9, s == "one"):
	}
	switch {
	case true:

		a, b := (func() (string, string) { return "a", "b" })()
		gobco4 := cond
		switch {
		case gobcoCover(10, gobco4 == true):
			a += b
			b += a
		}
	}

	ch := make(chan<- int, 1)
	switch {
	case true:
		ch <- 3
		gobco5 := expr
		switch {
		case gobcoCover(11, gobco5 == 5):
		}
	}

}

// :14:7: "expr == 5"
// :27:7: "s == \"one\""
// :28:3: "s == \"two\""
// :29:3: "s == \"three\""
// :36:7: "s + \"suffix\" == \"one\""
// :37:3: "s + \"suffix\" == \"two\""
// :38:3: "s + \"suffix\" == \"\" + s"
// :48:7: "s + \"suffix\" == \"prefix.a.suffix\""
// :52:7: "s + \"suffix\" == \"prefix.a.suffix\""
// :59:7: "s == \"one\""
// :67:7: "cond == true"
// :78:7: "expr == 5"
