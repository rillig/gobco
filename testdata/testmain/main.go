package testmain

// Without this file, gobco would not see the package at all.
// It would skip the test and also wouldn't write the coverage statistics.
// The gobco driver would then fail because it expects that file.
